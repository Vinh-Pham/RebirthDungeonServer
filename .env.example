# Drizzle tooling only; never copy these values into Worker bindings.
# db:check:remote is read-only. db:migrate:remote applies reviewed pending migrations.
CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_DATABASE_ID=
CLOUDFLARE_D1_TOKEN=
